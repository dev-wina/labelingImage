export enum PAGE_PATHS {
    HOME = "/annotator_home",
    DATASET_LABEL = "/dataset_label"
}

export enum PAINT_RECT_MODE {
    NONE = "none",
    CREATE = "createRect",
    MOVE = "moveRect",
    RESIZE_CORNER = "resizeRectCorner",
    RESIZE_ANCHOR = "resizeRectAnchor"
}

export enum KEYBOARD{
    BACKSPACE = 8,
    ENTER = 13,
    SPACEBAR = 32,
    DEL = 46
}