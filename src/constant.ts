export enum PAGE_PATHS {
    HOME = "/annotator_home",
    DATASET_LABEL = "/dataset_label"
}

export enum PAINT_RECT_MODE {
    NONE = "none",
    CREATE = "createRect",
    MOVE = "moveRect",
    RESIZE = "resizeRect"
}